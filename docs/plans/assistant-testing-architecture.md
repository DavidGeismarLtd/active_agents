# Assistant Conversation Testing - Architecture Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         PromptTracker                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐                    ┌──────────────┐          │
│  │ PromptVersion│◄───────┐           │  Assistant   │          │
│  └──────────────┘        │           └──────────────┘          │
│         △                │                   △                  │
│         │                │                   │                  │
│         │         ┌──────┴──────┐           │                  │
│         │         │   Testable  │           │                  │
│         │         │ (Polymorphic)│          │                  │
│         │         └──────┬──────┘           │                  │
│         │                │                   │                  │
│         └────────────────┼───────────────────┘                  │
│                          │                                      │
│                   ┌──────▼──────┐                              │
│                   │    Test     │                              │
│                   └──────┬──────┘                              │
│                          │                                      │
│                          │ has_many                             │
│                          │                                      │
│                   ┌──────▼──────┐                              │
│                   │  TestRun    │                              │
│                   │             │                              │
│                   │ - status    │                              │
│                   │ - passed    │                              │
│                   │ - conv_data │◄─── For assistants          │
│                   └──────┬──────┘                              │
│                          │                                      │
│                          │ has_many                             │
│                          │                                      │
│                   ┌──────▼──────┐                              │
│                   │ Evaluation  │                              │
│                   │             │                              │
│                   │ - score     │                              │
│                   │ - feedback  │                              │
│                   └─────────────┘                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Test Execution Flow

### For PromptVersion (Existing)

```
┌─────────────┐
│   Test      │
│ (testable:  │
│  PromptVer) │
└──────┬──────┘
       │
       │ run!(dataset_row)
       │
       ▼
┌─────────────────────┐
│ PromptVersion       │
│ .run_test()         │
└──────┬──────────────┘
       │
       │ delegates to
       │
       ▼
┌─────────────────────┐
│ PromptTestRunner    │
│                     │
│ 1. Render prompt    │
│ 2. Call LLM once    │
│ 3. Return response  │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ Run Evaluators      │
│ - Pattern match     │
│ - Length check      │
│ - Keyword check     │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ Create TestRun      │
│ - Single response   │
│ - Evaluations       │
└─────────────────────┘
```

### For Assistant (New)

```
┌─────────────┐
│   Test      │
│ (testable:  │
│  Assistant) │
└──────┬──────┘
       │
       │ run!(dataset_row)
       │
       ▼
┌─────────────────────┐
│ Assistant           │
│ .run_test()         │
└──────┬──────────────┘
       │
       │ delegates to
       │
       ▼
┌─────────────────────────────────────┐
│ AssistantTestRunner                 │
│                                     │
│ 1. Create thread                    │
│ 2. Initialize UserSimulator         │
│ 3. Loop (max_turns):                │
│    - Simulator generates message    │
│    - Send to assistant              │
│    - Collect response               │
│    - Check if done                  │
│ 4. Return conversation array        │
└──────┬──────────────────────────────┘
       │
       ▼
┌─────────────────────┐
│ Run Evaluators      │
│ - Conversation Judge│
│   (LLM evaluates    │
│    full convo)      │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ Create TestRun      │
│ - conversation_data │
│ - Evaluations       │
└─────────────────────┘
```

## 💬 Conversation Simulation Flow

```
┌──────────────────────────────────────────────────────────────┐
│                    Conversation Loop                          │
└──────────────────────────────────────────────────────────────┘

┌─────────────────┐
│ Dataset Row     │
│                 │
│ user_prompt:    │
│ "You are a      │
│  patient with   │
│  headache..."   │
└────────┬────────┘
         │
         ▼
┌─────────────────────────┐
│ UserSimulatorService    │
│                         │
│ - Persona: user_prompt  │
│ - Model: gpt-3.5-turbo  │
│ - Temp: 0.8             │
└────────┬────────────────┘
         │
         │ generate_message()
         │
         ▼
┌─────────────────────────┐
│ "Hi doctor, I have a    │
│  really bad headache"   │
└────────┬────────────────┘
         │
         │ send to assistant
         │
         ▼
┌─────────────────────────┐
│ OpenaiAssistantService  │
│                         │
│ - Assistant ID          │
│ - Thread ID             │
│ - Message               │
└────────┬────────────────┘
         │
         │ returns response
         │
         ▼
┌─────────────────────────┐
│ "I'm sorry to hear that.│
│  Can you describe the   │
│  pain for me?"          │
└────────┬────────────────┘
         │
         │ add to conversation
         │
         ▼
┌─────────────────────────┐
│ Conversation History    │
│                         │
│ [                       │
│   {role: "user", ...},  │
│   {role: "assistant",...│
│ ]                       │
└────────┬────────────────┘
         │
         │ feed back to simulator
         │
         ▼
┌─────────────────────────┐
│ UserSimulatorService    │
│ generate_message(       │
│   conversation_history) │
└────────┬────────────────┘
         │
         │ "It's on the right
         │  side, throbbing..."
         │
         ▼
      [LOOP CONTINUES]
         │
         │ until:
         │ - max_turns reached
         │ - simulator returns nil
         │ - CONVERSATION_COMPLETE
         │
         ▼
┌─────────────────────────┐
│ Final Conversation      │
│                         │
│ 6 turns                 │
│ Natural conclusion      │
└─────────────────────────┘
```

## 🎭 Role Flipping in User Simulator

```
┌──────────────────────────────────────────────────────────────┐
│         How UserSimulator Flips Roles                         │
└──────────────────────────────────────────────────────────────┘

Actual Conversation:
┌─────────────────────────────────────────────────────────────┐
│ USER: "I have a headache"                                    │
│ ASSISTANT: "Can you describe it?"                            │
│ USER: "It's throbbing"                                       │
└─────────────────────────────────────────────────────────────┘

Passed to UserSimulator (roles flipped):
┌─────────────────────────────────────────────────────────────┐
│ SYSTEM: "You are a patient with headache..."                │
│ ASSISTANT: "I have a headache"        ← Was USER             │
│ USER: "Can you describe it?"          ← Was ASSISTANT        │
│ ASSISTANT: "It's throbbing"           ← Was USER             │
└─────────────────────────────────────────────────────────────┘

Why? Because from the simulator's perspective:
- It's generating USER messages (so its output = ASSISTANT role)
- It's responding to ASSISTANT messages (so input = USER role)
```

## 🎯 Conversation Judge Evaluation

```
┌──────────────────────────────────────────────────────────────┐
│              Conversation Judge Flow                          │
└──────────────────────────────────────────────────────────────┘

Input:
┌─────────────────────────┐
│ Conversation Array      │
│ [                       │
│   {role: "user", ...},  │
│   {role: "assistant",...│
│   ...                   │
│ ]                       │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Format as Text          │
│                         │
│ USER: I have a headache │
│ ASSISTANT: Can you...   │
│ USER: It's throbbing... │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Build Judge Prompt      │
│                         │
│ "Evaluate this convo... │
│  Criteria:              │
│  - Empathy              │
│  - Completeness         │
│  - Safety               │
│  Respond in JSON..."    │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Call LLM Judge          │
│ (gpt-4, temp: 0.3)      │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Parse JSON Response     │
│                         │
│ {                       │
│   overall_score: 92,    │
│   criteria_scores: {...}│
│   feedback: "...",      │
│   passed: true          │
│ }                       │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Create Evaluation       │
│                         │
│ - score: 92             │
│ - passed: true          │
│ - feedback: "..."       │
└─────────────────────────┘
```

## 📊 Data Model Relationships

```
PromptVersion ──┐
                │
                ├──► Testable (polymorphic)
                │
Assistant ──────┘
                │
                │
                ▼
              Test ──────► EvaluatorConfig
                │              │
                │              │ (conversation_judge)
                │              │
                ▼              ▼
            TestRun ──────► Evaluation
                │
                │
                ▼
          conversation_data: [
            {role: "user", content: "..."},
            {role: "assistant", content: "..."}
          ]
```

---

**These diagrams provide a visual guide to understanding the assistant testing architecture.** 🎨

