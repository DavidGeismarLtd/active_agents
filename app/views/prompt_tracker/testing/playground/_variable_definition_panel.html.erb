<%# Variable Definition Panel %>
<div class="card mb-3">
  <div class="card-header">
    <h5 class="mb-0">
      <i class="bi bi-input-cursor-text"></i>
      Variables
    </h5>
  </div>
  <div class="card-body">
    <div id="variables-container" data-playground-editor-target="variablesContainer">
      <% if sample_variables.any? %>
        <% sample_variables.each do |name, value| %>
          <div class="mb-2">
            <label for="var-<%= name %>" class="form-label"><code><%= name %></code></label>
            <input
              type="text"
              class="form-control variable-input"
              id="var-<%= name %>"
              data-variable-name="<%= name %>"
              value="<%= value %>"
              placeholder="Enter value for <%= name %>"
              data-action="input->playground-editor#onVariableInput"
            >
          </div>
        <% end %>
      <% else %>
        <p class="text-muted">No variables detected. Start typing in the template editor.</p>
      <% end %>
    </div>
  </div>
</div>

