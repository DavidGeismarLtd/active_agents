<% content_for :breadcrumbs, flush: true do %>
  <li class="breadcrumb-item"><%= link_to "Prompts", testing_agents_path %></li>
  <li class="breadcrumb-item"><%= link_to "v#{@version.version_number}", testing_agent_agent_version_path(@prompt, @version) %></li>
  <li class="breadcrumb-item"><%= link_to "Tests", testing_agent_version_tests_path(@version) %></li>
  <li class="breadcrumb-item"><%= link_to @test.name, testing_agent_version_test_path(@version, @test) %></li>
  <li class="breadcrumb-item active">Edit</li>
<% end %>

<div class="row mb-4">
  <div class="col">
    <h1>
      <i class="bi bi-pencil"></i> Edit Test
      <span class="badge bg-primary">v<%= @version.version_number %></span>
    </h1>
    <p class="text-muted">Update test configuration for <%= @test.name %></p>
  </div>
  <div class="col-auto">
    <%= link_to testing_agent_version_test_path(@version, @test), method: :delete, data: { confirm: "Are you sure you want to delete this test?" }, class: "btn btn-outline-danger" do %>
      <i class="bi bi-trash"></i> Delete Test
    <% end %>
  </div>
</div>

<%= render "form", prompt: @prompt, version: @version, test: @test %>
